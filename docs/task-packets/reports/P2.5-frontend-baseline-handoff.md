STATUS: done
PACKET: docs/task-packets/CURRENT-PACKETS.md -> P2.5-frontend-baseline
BASELINE: HEAD=2bc1c0d（P2-integration CR blocker-fix 已 approve）。开工时保留既有 `docs/task-packets/CURRENT-PACKETS.md` 修改；`demo/`、`experiments/` 未跟踪且全程只读。本包未提交。第 1 轮 Review 后已返工（新增 `fixture-composition.ts` 组合解析层与默认仓/组合边界回归），本正文已刷新至返工后状态；第 1、2 轮复审记录见文末 `## Review`。

Contract ledger:
- CURRENT-PACKETS.md P2.5-frontend-baseline — 交付 AppShell/ReaderShell/router、五个入口与设置、素材详情/阅读器/Study 骨架；P1/P2 面继续使用真实 API，P3+ 无后端面使用明确 unstable fixture adapter；提取/确认只显示待实现。
- DESIGN.md 信息架构、组件、无障碍与响应式行为 — 顶级入口命名为“主页／素材库／知识库／解析队列／学习中心”，设置固定在导航底部；Reader 在 1180/760 两个断点切换 rail、drawer、sheet，Study 窄屏在文档/对话之间切换并保留 return context。
- data-model §§0、2、5、7、8 与 ADR-009/023/032/040/041 — 文本 Span 使用 Unicode code point 半开区间；组件不依赖 shell/router；词典／分析器来源和版本分开；KP、Occurrence、ReviewItem、KnownEvidence 职责分离；queued/active/paused/retired 不互相冒充。
- P2-frontend/P2-integration handoff（含 Review） — 172 例 P2 回归是不可回退基线；reader fixture 仍是唯一跨边界 artifact；查词裁定竞态、模态边界和请求版本令牌未重写。
- 不能假设 — 当前 OpenAPI 没有 StudySession、KnowledgePoint 聚合或 Review 排程端点；本包不发明这些请求、不使用 demo tokenizer/localStorage/reducer 作为业务状态源、不实现提取/确认/FSRS/Anki。
- Owned files — 仅 `frontend/**` 与本包 handoff 报告；`backend/**`、`DESIGN.md`、其他 `docs/`、`prompts/`、`demo/`、`experiments/` 均为只读或既有变更，不在本包改动范围内。
- Blockers — 无；P2-integration CR blocker-fix 已 approve，开工基线与本包验收门禁均通过。

Changed:
- `frontend/src/app/AppRouter.tsx`、`routes.ts`、`theme.tsx`、`repository-context.tsx` — React Router 路由树、入口命名/Study return-context 编码、主题与 repository 装配；默认 bundle 让 P1/P2 使用 API adapter，P3+ 使用 fixture adapter。
- `frontend/src/shells/AppShell.tsx`、`AppShell.test.tsx`、`ReaderShell.tsx`、`ReaderShell.test.tsx` — 桌面/紧凑/窄屏导航和全屏 Reader 工具栏，侧栏/抽屉/底部面板布局由 shell 决定，并以三档视口回归锁定单一面板渲染；<760px 设置位于独立底部导航区（`AppShell.test.tsx` 锁定），ReaderShell 不设查词入口、查词归属 MaterialWorkspace（`ReaderShell.test.tsx` 锁定）。
- `frontend/src/screens/Page.tsx`、`HomeScreen.tsx`、`LibraryScreen.tsx`、`MaterialDetailScreen.tsx`、`ReaderScreen.tsx`、`QueueScreen.tsx`、`KnowledgeScreen.tsx`、`CenterScreen.tsx`、`SettingsScreen.tsx`、`StudyScreen.tsx` — 五个入口、素材详情、Reader、学习中心与 Study 诚实骨架；页面通过 context 读取 repository，P3+ 状态显式标 fixture/待实现。
- `frontend/src/shells/MaterialWorkspace.tsx` — 保留 P2 既有查词/裁定实现并增加 `initialMaterialId`、`initialSentenceId`、`hideLibrary` 与可注入 Material/Reader repository，使 Reader route 能复用真实 P1/P2 阅读区而不嵌套素材库，并从 `?s=` 精确恢复来源句。裁定请求 `material_id` 改取当前选中材料（真实 API 下与 token provenance 恒等；`MaterialWorkspace.test.tsx` 对应断言同步更新一处）。
- `frontend/src/components/MaterialCard.tsx`、`ChapterList.tsx`、`SessionList.tsx`、`HighlightedText.tsx`、`AnalysisDocument.tsx`（含 `AnalysisDocView` 别名）、`AgentConversation.tsx`、`ConfirmationPanel.tsx`、`KnowledgeAggregate.tsx`、`ReviewCard.tsx`、`PageHeader.tsx`、`models.ts`、`ui/badge.tsx`、`ui/button.tsx` — 从 demo 迁移并整理为 props/callback 驱动的 shell-independent 组件；Highlight 使用 `sliceByCodePoint`；提取/确认与评分均无隐式写入。
- `frontend/src/components/MigratedComponents.test.tsx` — 组件迁移、Span code-point、状态标签、回调边界回归。
- `frontend/src/lib/annotations.ts`、`material-repository.ts`、`reader-repository.ts`、`study-repository.ts`、`knowledge-repository.ts`、`review-repository.ts`、`repository-utils.ts`、`repositories.ts`、`fixture-composition.ts`、`repository-adapters.test.ts` — 五个 repository 与 API/fixture adapter，以及 P2 Annotation 只读 API 投影；P3+ API adapter 为不发请求的 capability placeholder，接口标 `unstable: true`。`fixture-composition.ts` 把 P1 material fixture 与 P2 reader fixture 两份独立生成 artifact 组合进 `fixture-ui-*` 应用本地命名空间，并提供默认仓使用的 `FixtureResolvingMaterialRepository`（真实素材保持 API 读取；组合 ID 直接读组合 fixture；原始 `fixture-id-*` 仅在内容哈希证明一致时解析为组合身份）与 `FixtureResolvingReaderRepository`（P1/P2 端点仍接收生成的原始 ID，组合 ID 在端点边界重写、响应 provenance 重写回组合命名空间）；`reader-fixtures.ts` 相应新增 srt token fixture 与 annotation fixture。Knowledge fixture 将 reference 保留在 Occurrence 层，KP 默认仍符合未有决定记录时的初值语义。
- `frontend/src/App.tsx`、`frontend/src/index.css`、`frontend/package.json`、`frontend/pnpm-lock.yaml` — 接入 Theme/Router/Repository provider、补 AI 语义色；仅新增允许依赖 `react-router-dom` 与 demo 已使用的 `lucide-react`。
- `frontend/README.md` — 更新目录边界与“尚未接入”说明，明确 P2.5 已接入 Router/repository/P1-P2 API，后续能力仍以本报告为准。
- `frontend/src/app/AppRouter.test.tsx` — 用 MemoryRouter + fixture repository 验证入口标签、队列与全屏 Study 路由装配；另加默认仓（Material/Reader API + P3+ fixture）路由回归：默认路由零 fetch 解析 fixture 来源句、内容哈希匹配的素材库可达 Study、Reader 组合 ID 的端点重写边界。

Public contract:
- 正式路由：`/`、`/library`、`/material/:materialId`、`/knowledge`（可选 `?material=`）、`/queue`（可选 `?material=`）、`/center`、`/settings` 均由 `AppShell` 装配；`/reader/:materialId?s=` 使用全屏 `ReaderShell`；`/study/:sessionId?from=&material=&sentence=&return=` 使用全屏 Study 工作区。Study 的素材库、解析队列、学习中心入口分别写入 `from` 与 `return`，返回按钮按该上下文回到原来源。
- `MaterialRepository`：`listMaterials/getMaterial/listSentences/getSidecar/getLexemeCounts`；`MaterialApiAdapter` 复用既有 P1 客户端，`MaterialFixtureAdapter` 只提供 P1 fixture；默认 bundle 的 materials 为 `FixtureResolvingMaterialRepository`（API 优先，`fixture-ui-*` 组合命名空间离线可读，原始 `fixture-id-*` 须内容哈希证明）。
- `ReaderRepository`：`getSentenceTokens/listDictionaries/lookupDictionary/searchDictionary/getEvidenceSummary/recordLexemeDecision/listAnnotations`；`ReaderApiAdapter` 复用既有 P2 客户端（含 Annotation 列表），`ReaderFixtureAdapter` 只读 fixture 并保留 expected sequence 与幂等裁定；默认 bundle 的 reader 为 `FixtureResolvingReaderRepository`，组合 ID 在端点边界重写为 P1 material fixture 数据集的原始 ID（`sentenceEndpointIds` 与 `materialEndpointIds` 同锚一个数据集，第 3 轮复审确认全链路自洽并有 adapter/路由两级回归）。
- `StudyRepository`（unstable）：`listSessions/getSession/listActiveSessions/listHistory`；`StudyFixtureAdapter` 覆盖 preparation、discussion、parked、completed 及 extraction/confirmation 的只读待实现状态；`StudyApiAdapter` 在后端端点出现前显式失败。
- `KnowledgeRepository`（unstable）：KP/Occurrence 查询与 aggregate 查询，`KnowledgeFixtureAdapter` 保留 anchor、section revision、Span 与 retention；`KnowledgeApiAdapter` 不发 phantom 请求。
- `ReviewRepository`（unstable）：ReviewItem 查询，fixture 明确区分 queued/active/paused/retired 且不伪造 ReviewState；`ReviewApiAdapter` 仅作能力占位。
- 组件 props 面：`MaterialCard`、`ChapterList`、`SessionList`、`AnalysisDocument/AnalysisDocView`、`AgentConversation`、`ConfirmationPanel`、`KnowledgeAggregate`、`ReviewCard` 均只接收数据和回调，组件目录没有 shell/router 反向依赖。

Fixture/API boundary:
- 素材、Sentence、sidecar、lexeme counts、算法 token、词典、Lexeme evidence/decision 仍由既有 P1/P2 API adapter 提供；Reader route 通过 `MaterialWorkspace` 使用同一真实 API，未接入 demo `fixtures/tokenize.ts`。
- P3+ Study/Knowledge/Review 没有当前后端端点，context 默认显式使用 fixture adapter；fixture 读取会 clone 数据、支持 abort，不读取 demo localStorage/reducer。对应 API adapter 返回 `FixtureCapabilityError`，不会生成未存在的 HTTP 调用。
- fixture 状态仅用于已迁移屏幕：loading/empty/error/retry、preparation、discussion、parked、queued/active/paused/retired。extraction/confirmation 仅呈现阶段标签和“待实现”占位，未实现候选、部分确认、零候选、提取重试或计时器。
- 所有持久 Span 继续用 code-point 半开区间；`HighlightedText` 通过 `sliceByCodePoint` 渲染，静态裸 slice 扫描只命中工具内部数组切片。

Handoff points:
- P3a-frontend：将 `StudyApiAdapter` 替换为 StudySession/任务队列/AnalysisRevision/只读 Q&A API；保留 `StudyScreen` 的 source/return wiring、`AnalysisDocument`、`AgentConversation` 和 preparation/discussion/paused 视觉状态，接入真实 run status，不添加前端 timer。
- P3b-frontend：在同一 Study shell 上接 AgentRun/ToolCall、revisioned 文档编辑、笔记与冲突/预算暂停；复用既有文档、对话组件，通过回调展示 committed/conflict/partial failure，不重建路由或复制写入路径。
- P4a-frontend：把 `ConfirmationPanel` 的占位替换为固定 AnalysisRevision 的 ExtractionRun/ExtractionSection/Span 诊断面；保留失败/部分候选的明确状态，但仍不在组件内推导偏移或身份。
- P4b-frontend：接入逐 Occurrence 确认；`KnowledgeAggregate` 的 KP 折叠只能作为展示，逐项 retention 与 `SessionConfirmation` 必须由 shell/repository 回调承载，inherit 未确认与 reference/安排复习保持区分。
- P5-frontend：将 `KnowledgeFixtureAdapter`/`ReviewFixtureAdapter` 换成真实聚合、配额与 ReviewItem/State/Event adapter；保留学习中心的 queued/active/paused 计数和 `ReviewCard` 视觉边界，分别接入站内排程、卡片预览、Anki 导出确认与外部发送结果。

Dependencies:
- `react-router-dom@^7.18.3` — 提供 HashRouter、Routes、NavLink、Link、useParams/useSearchParams/useNavigate，承载正式入口、Reader/Study 全屏路由与 return context，并保留 demo 的 hash-link 可寻址方式。
- `lucide-react@^1.45.0` — 提供 AppShell/ReaderShell 和页面中的单色导航、状态与来源图标。
- 本包未新增 Radix Dialog/Dropdown/Popover/ScrollArea/Slot/Tabs/Tooltip、`class-variance-authority` 或其他允许清单项；UI 原语保持原生 props/callback，后续如需引入须遵守清单。未引入 TanStack Query/Zustand。

Verified:
- `cd frontend && pnpm test` — pass，`26` 个文件、`206` 个测试（第 2 轮复审实测）；其中原 P2 基线 `172` 例全部保留且 `p2-integration.contract.test.ts` 未修改。
- `cd frontend && pnpm lint` — pass，覆盖 `learningj/no-bare-string-slice` 与组件 shell/router import 双栅栏。
- `cd frontend && pnpm build` — pass，`tsc -b && vite build` 成功。
- `cd backend && uv run pytest -q` — pass，`142 passed, 21 xfailed, 1 warning`；backend 未改动。
- `cd demo && pnpm typecheck && pnpm build` — pass；demo/ integrity gate 通过，experiments/ 全程未触碰。
- 静态边界检查 — component→shell/router 零命中；frontend 无 demo tokenizer/fixture import 到真实素材组件；无 TanStack Query/Zustand；`git diff --check` pass。

Known limitations:
- 原第 2 轮 P1（组合 ID 跨数据集拼接）已由第 3 轮返工修复并复审通过：`sentenceEndpointIds` 锚定 P1 material fixture（文本 `fixture-id-004`、字幕 `fixture-id-008`），Study/Knowledge/Annotation 使用独立 P1 sentence 映射，reader artifact 身份仅保留于离线 fixture；adapter 与路由两级测试以单一 P1 自洽数据集锁定全链路（见 `## Review` 第 3 轮）。残余：`annotationSentence`、第三句与 epub 句无 token 捕获，Reader 对其显式 404/错误态。
- 素材导入、材料管理、Annotation/播放器、章节后端接口和真实阅读 return_position 写入尚未接入；详情页章节目前是“当前已导入 Sentence”的最小投影，未发明章节实体。
- Reader route 复用 P2 `MaterialWorkspace` 的真实阅读/查词区；ReaderShell 的目录/队列/阅读设置目前是诚实的待实现提示，查词仍由 P2 面板按视口选择 drawer/popover/sheet。
- Study 没有真实 provider、任务队列、文档编辑或 Q&A 写入；Agent 输入、提取候选、确认与重试均不可用。extraction/confirmation fixture 只用于标识边界，不代表后端行为已存在。
- Knowledge/Review 目前只读 fixture；没有 KP/Occurrence 意愿写入、ReviewState/ReviewEvent、FSRS 排程、配额准入或 Anki 导出/外部发送。
- P3+ API adapter 是显式 capability error，占位而非兼容实现；后续包必须以真实 OpenAPI 替换，不应把 fixture 结果当生产数据。
- 工作树既有 `docs/task-packets/CURRENT-PACKETS.md` 修改与未跟踪 demo/、experiments/ 均保留，未执行 git 操作或回滚。

Gate for next packet:
- Ready: P3a/P3b 可在不改动组件归属的前提下替换 Study adapter，P4a/P4b 可沿固定 revision/Occurrence 粒度替换提取与确认，P5 可替换 Knowledge/Review adapter；五个入口、ReaderShell、Study return context 和 P2 172 例回归均有新鲜命令证据（第 3 轮复审后 P1 已闭合，fixture 来源 Reader 深链在持有 material fixture 的后端上全链路可用）。
- Not safe to assume: 任何 P3+ 真实 API、Agent 工具循环、ExtractionRun/SessionConfirmation、ReviewItem 准入/FSRS、Anki 互操作、章节/导入/Annotation UI；也不要把 fixture 的阶段状态或 `return_position` 展示解释为真实后台进度或阅读活动。

CR focus:
- 检查路由/外壳边界是否保持：AppShell 仅包五个入口与设置，Reader/Study 全屏；Study 的 `from/return` 不丢失来源语境。
- 检查五个 repository 的 source/unstable 元数据和 API placeholder 不被误接到 fixture-only 页面；Material/Reader API adapter 是否仍复用 P1/P2 客户端。
- 检查 P2 `MaterialWorkspace` 的 `initialMaterialId/hideLibrary` 变更未改变 172 例查词、裁定竞态、模态与 code-point 回归。
- 检查组件目录零路由/shell 反向依赖、无 demo tokenizer、无裸字符串切片，以及新增依赖仅为允许清单。

Open issues:
- None（本包验收命令全部通过；按指示不执行 git commit，交由 lead 复核后入册）。

## Review

### 第 1 轮（CR，reviewer；原文保留）

VERDICT: changes-requested
Findings:
- [P1] `frontend/src/app/repository-context.tsx:26-34`、`frontend/src/lib/study-repository.ts:202-310`、`frontend/src/screens/QueueScreen.tsx:50-57`、`frontend/src/screens/StudyScreen.tsx:62-69`、`frontend/src/screens/KnowledgeScreen.tsx:59-67`、`frontend/src/screens/CenterScreen.tsx:70-87` — 默认 bundle 把真实 `MaterialApiAdapter` 与使用 `fixture-id-*` 合成材料/句子 ID 的 P3+ Study/Knowledge/Review fixture 混接；真实 API 素材不可能命中这些 ID，导致默认 `/queue`、`/study`、`/knowledge`、`/center` 在读取来源句时进入 404/error，真实素材库/详情页也找不到对应 fixture 会话，Study 无法从素材库入口到达。违反 `CURRENT-PACKETS.md` P2.5 Required delivery（Study 从素材库/解析队列/学习中心可达）、DESIGN 信息架构与 data-model §4.1 的 material/sentence 作用域。最小修复：为 fixture 屏幕提供同一组自洽的 fixture material/sentence resolver（保留 Reader/Material 的 P1/P2 真实 API adapter），或在 adapter 能证明 ID 对应关系前显式隐藏 fixture 会话/来源链接；补一条使用默认 `App` 仓装配的路由集成测试锁定该边界。
- [P1] `frontend/src/lib/reader-fixtures.ts:36-45`、`frontend/src/lib/material-fixtures.ts:21-95`、`frontend/src/lib/reader-repository.ts:168-174,210-214`、`frontend/src/lib/material-repository.ts:136-147` — `createFixtureRepositories()` 组合了互不相容的跨 artifact 身份：P1 fixture 中 `fixture-id-003` 属于 material `fixture-id-001`/sidecar `fixture-id-002`，而锁定的 reader fixture 中同一 sentence 的 token response 声明 material `fixture-id-004`/sidecar `fixture-id-005`；srt token 与 annotation 也指向 P1 fixture 不存在的 material/sentence。`MaterialWorkspace` 还在 `frontend/src/shells/MaterialWorkspace.tsx:370-378` 直接把 token response 的 `material_id` 写入裁定请求，因此 fixture 路径会发送错误的材料作用域。违反 data-model §8.2–§8.3、P2 OpenAPI provenance/sidecar generation 语义以及本包“fixture 不得创造第二套领域契约”要求。锁定的 `reader-fixture.json` 不应修改；最小修复是增加显式、确定性的 fixture composition/mapping，使 material、sentence、sidecar generation 和 annotation 关系在 adapter 边界内自洽，并用 repository contract test 断言这些关系。
- [P2] `frontend/src/shells/AppShell.tsx:77-90` — `<760px` 的导航把“设置”作为顶部横向导航的最后一项渲染，未满足 DESIGN.md 信息架构与应用外壳条款要求的“设置入口固定在导航栏底部”。最小修复：为窄屏保留独立的底部设置区域（或等价的底部锚定导航项），并补三档视口的 DOM/可访问性断言。
- [P2] `frontend/src/screens/ReaderScreen.tsx:47-56`、`frontend/src/shells/ReaderShell.tsx:225-249`、`frontend/src/shells/MaterialWorkspace.tsx:465-499` — Reader 工具栏的“查词”先由 `ReaderShell` 打开一个占位侧栏；用户再点 token 时，`MaterialWorkspace` 又打开自己的 P2 `LookupSurface`。桌面可同时出现两个独立查词面板（两套 open/close 状态），违反 DESIGN.md“同一容器原地更新同一个查词界面”和 ReaderShell/组件归属边界。最小修复：让 ReaderShell 的查词按钮只驱动 MaterialWorkspace 的唯一 surface，或移除 ReaderScreen 的独立查词 sidebar，并以单一状态/回调装配。

Evidence checked:
- HEAD 为 `2bc1c0d`；读取 `docs/task-packets/protocols/code-review.md`、`docs/task-packets/protocols/implementation.md`、`docs/task-packets/CURRENT-PACKETS.md` P2.5 行、P2-frontend/P2-integration handoff 及其既有 `## Review`，以及 P2.5 Mandatory reading 的 plan §15/§3、DESIGN.md、data-model §§0/2/5/7/8、ADR-009/023/032/040/041、frontend/README.md、demo/README.md。
- 当前 diff（`git diff HEAD --stat/name-only`）与所有新增 `frontend/src` 源码/测试逐文件核对；owned 变更仅在允许的 frontend/** 与本包报告范围，既有 `CURRENT-PACKETS.md` 修改保持不变。
- `cd frontend && pnpm test` — pass，25 files / 197 tests；P2 172-test baseline（含锁定 `p2-integration.contract.test.ts`）仍通过。
- `cd frontend && pnpm lint` — pass；`cd frontend && pnpm build` — pass（`tsc -b && vite build`）。
- `cd backend && uv run pytest -q` — pass，142 passed / 21 xfailed / 1 warning；backend 未修改。
- `cd demo && pnpm typecheck && pnpm build` — pass；demo/ 与 experiments/ 未执行 git 操作。
- `git diff --check` — pass；`git status --short` 显示既有 `CURRENT-PACKETS.md` 修改、frontend 改动、本报告及未跟踪 demo/、experiments/，未发现其它范围变更。
- component → shells/react-router import scan — zero；实际 demo tokenizer import scan — zero（仅 `reader-repository.ts:137` 的说明注释命中）；bare string `slice/substring/substr` scan 仅命中授权的 `frontend/src/lib/text.ts:46` 数组切片及测试注释/数组切片；TanStack Query/Zustand scan — zero。
- 保护边界 `git diff HEAD -- backend DESIGN.md prompts frontend/src/lib/p2-integration.contract.test.ts backend/fixtures/reader-fixture.json` — zero；只读关系脚本确认 P1 material fixture 的 materials=`fixture-id-001/006`，而 reader `tokens/txt` 为 material=`fixture-id-004`、sidecar=`fixture-id-005`，reader `tokens/srt` 为 material=`fixture-id-007`、sentence=`fixture-id-006`，annotation 指向 material=`fixture-id-004`/sentence=`fixture-id-011`。

Gate assessment:
- P2.5 本包不能 approve，P3a/P3b/P4a/P4b/P5 不安全直接接手：虽然静态组件边界、P2 lookup/decision 回归、构建与后端/ demo 基线均通过，但默认仓装配与 fixture 身份边界会使 P3+ 屏幕在真实 API bundle 下无法读取来源，且 fixture 裁定会落到错误 material scope。先修复两项 P1 并补默认仓与 fixture composition 回归；移动设置和双查词面板随后修复。修复后再复跑五组命令与边界扫描。

Residual risks:
- 本轮未做独立浏览器像素级截图/触摸实测；紧凑 popover 仍是阅读区右上角近似锚定，沿用 handoff 已记录的非阻断风险。
- 真实后端 409 并发裁定未演练；现有客户端/组件竞态测试与既有 P2 Review 证据仍通过。

### 2026-09-13 第 2 轮复审（Rework 后，CR，reviewer）

VERDICT: changes-requested
Findings:
- [P1] `frontend/src/lib/fixture-composition.ts:80-83`（对照 `:72-78`，重写点 `:269-284`、`:306`） — 默认仓 reader 重写把两个互斥生成数据集的 `fixture-id-*` 身份拼接成一条请求路径：`readerSentenceIds` 采用锁定 reader artifact（独立重建的 P2 数据集）的句子身份（`fixture-ui-text-sentence→fixture-id-003`、`fixture-ui-subtitle-sentence→fixture-id-006`），而 `materialEndpointIds` 与 `resolveFixtureAlias` 的内容哈希证明采用 P1 material fixture 数据集的材料身份（`fixture-ui-text-material→fixture-id-001`、`fixture-ui-subtitle-material→fixture-id-006`）。实测锁定 artifact：material-fixture.json 中 `fixture-id-003`=「𠮟られた。」、`fixture-id-006`=srt 材料；reader-fixture.json 中 `fixture-id-003`=「次は君の番です！」的 token 句、`fixture-id-001`=词典来源——同一原始 ID 在两数据集指向不同实体，不可能共存于一个数据库。在与 material 侧证明相容的同一后端（含 P1 material fixture，即 `listMaterials` 哈希交换与裁定 `material_id='fixture-id-001'` 能成立的那一个）上，默认 bundle 打开 `/reader/fixture-ui-text-material?s=fixture-ui-text-sentence` 会静默渲染「𠮟られた。」的 tokens（选句与 Study 来源语境均为「次は君の番です！」），subtitle 来源则 `GET /sentences/fixture-id-006/tokens` 404；反之在 reader 数据集上每条 Lexeme 裁定 POST 都指向不存在的材料。这违反 CURRENT-PACKETS.md P2.5 交付项「fixture 不得创造第二套领域契约／Study-Reader 来源可达」与 data-model §8 身份/代次作用域，且现有测试把矛盾固化：`frontend/src/lib/repository-adapters.test.ts:242-268` 与 `frontend/src/app/AppRouter.test.tsx:120-138` 分别用 P2 身份 mock tokens、用 P1 身份 mock materials/annotations，任何单一自洽数据集下的行为从未被断言。最小修复：将 `readerSentenceIds` 锚定到与 `materialEndpointIds` 相同的 P1 数据集身份（textSentence→`fixture-id-004`、subtitleSentence→`fixture-id-008`，两者表面形与组合句一致），或对 token 响应增加表层证明（拼接 surface 与 fixture 句文本一致才接受，否则显式报错），并把上述两个测试改为对同一自洽数据集断言 tokens+materials+decisions 全链路。
- [P2] `docs/task-packets/reports/P2.5-frontend-baseline-handoff.md:14-24,26-39,53-59` — 报告正文停留在第 1 轮实现状态：Verified 记录 25 文件/197 测试（本轮实测 26 文件/206 测试），Changed 与 Public contract 未提及本轮返工核心 `fixture-composition.ts`、`FixtureResolvingMaterialRepository`/`FixtureResolvingReaderRepository` 及对应 AppRouter/repository-adapters 回归。`protocols/handoff.md` 要求报告列出真实变更与验证结果，后续包以该报告为接手依据。最小修复：由 lead/实现方刷新正文 Changed/Verified/Public contract 以描述解析器组合与最新命令证据（本轮按审查纪律未改动正文）。
Evidence checked:
- HEAD=`2bc1c0d`；读取 `protocols/code-review.md`、`protocols/implementation.md`、CURRENT-PACKETS.md P2.5 行、P2-frontend/P2-integration handoff（含既有 Review）、plan §15（§15.14 入口命名、§15.23 主页占位）、DESIGN.md（应用外壳/信息架构/组件/无障碍/响应式）、data-model §0/§2/§4.1/§7.1/§8、ADR-009/023/032/040/041 要点、frontend/README.md、demo/README.md。
- 逐文件核对当前 diff 与全部新增 `frontend/src` 源码/测试（app/ 路由与 context、screens/ 九屏、AppShell/ReaderShell 及测试、迁移组件与 MigratedComponents.test、五个 repository + fixture-composition + repository-adapters.test、MaterialWorkspace diff）；锁定 artifact 实测比对 material-fixture.json 与 reader-fixture.json 的全部 `fixture-id-*` 身份。
- Round-1 四项 finding 复核：#1 关闭（`fixture-ui-*` 命名空间 + `AppRouter.test.tsx:72-98` 证明默认路由 /queue、/study、/knowledge、/center 零 fetch 解析来源 + Study 从素材库 `LibraryScreen.tsx:122`、队列、中心可达且 `from/return/material/sentence` 与 `returnTarget` 同源守卫不丢语境）；#3 关闭（`AppShell.tsx:122-135` 独立底部设置导航，`AppShell.test.tsx:14-25` 锁定，桌面侧栏设置位于 `mt-auto` 底部）；#4 关闭（ReaderShell 无查词入口，仅目录/解析队列/阅读设置，`ReaderShell.test.tsx:45` 锁定查词归属 MaterialWorkspace，P2 LookupSurface 为唯一查词面）；#2 部分关闭（离线组合自洽且有 provenance 测试 `repository-adapters.test.ts:204-227`，默认仓 reader 重写仍跨数据集拼接 → 本轮 finding 1）。
- `cd frontend && pnpm test` — pass，26 files / 206 tests（含 P2 基线 172 例；`p2-integration.contract.test.ts` 零改动；`MaterialWorkspace.test.tsx` 仅 1 处断言随 `material_id: selected?.id`（`MaterialWorkspace.tsx:381`，真实 API 下与 token provenance 恒等）同步更新 `:290`）；`pnpm lint` exit 0；`pnpm build` pass（`index-BeEK-5Y5.js 365.12 kB`）。
- `cd backend && uv run pytest -q` — pass，142 passed / 21 xfailed / 1 warning；`cd demo && pnpm typecheck && pnpm build` — pass；demo/、experiments/ 未执行任何 git 操作。
- `git status --short`/`git diff --check` — pass；改动仅在 frontend/**、本报告与既有 CURRENT-PACKETS.md 修改；`git diff HEAD -- backend DESIGN.md prompts docs/LearningJ-plan-v5.md docs/data-model.md frontend/src/lib/p2-integration.contract.test.ts backend/fixtures/reader-fixture.json` 为零。
- 边界扫描：component→shells/react-router import 零命中（screens→shells 仅 ReaderScreen 按所有权方向组装）；demo tokenizer 仅 `reader-repository.ts:121` 注释命中；裸 slice 仅 `text.ts:46` 授权的 code-point 数组切片；TanStack/Zustand/setInterval/setTimeout 零命中；localStorage 仅 `theme.tsx` 外观偏好（非业务状态源）；新增依赖仅 `react-router-dom@^7.18.3` 与 `lucide-react@^1.45.0`（package.json 实测一致）。
- 领域语义抽查：StudySession status/phase、ReviewItem 四态与 admitted_at/retired_at 约束、Occurrence retention_override 与 KP default_retention 初值、`return_position` 仅导航语义、code-point Span（`MigratedComponents.test.tsx:23`）均与 data-model 一致；extraction/confirmation 仅显式待实现占位（`ConfirmationPanel.tsx`、QueueScreen/StudyScreen 标注），无 timer 伪进度、无 phantom API（P3+ API adapter 为显式 `FixtureCapabilityError`）。
Gate assessment:
- P3a/P3b/P4a/P4b/P5 暂不安全直接接手：组件归属、shell 独立性、repository/adapter 边界、五入口命名、Study return context、fixture 状态矩阵与 P2 172 例回归均已达标且有新鲜命令证据，但 finding 1 的身份矛盾正位于 P3+ 接手点中央——Study/Queue/Center 的「回到来源」链接与 P3a 的 Reader 复用都走这条重写路径，且现有测试以混合数据集 mock 掩盖了它；须先修复并补自洽数据集回归后再派遣。finding 2 由 lead 刷新报告正文即可，不阻塞代码派工。
Residual risks:
- 本轮未在真实后端上做浏览器级点通：dev DB 上的 token 错配由两份锁定 artifact 的身份直接推导（结论不依赖运行环境），但未在本机重建库复演实际渲染；P2 遗留的 popover 近似锚定与 409 并发裁定未演练继续挂起。
- CenterScreen 以 `analysis-{sessionId}` 命名约定把 Occurrence 关联回会话（fixture 内部约定，`CenterScreen.tsx:112`）；P3a 接入真实关系时必须替换，不应沿用。
- experiments/ 内容未检查（只读未跟踪来源；其完整性门禁为 demo typecheck+build，已通过）。

### 2026-09-13 第 3 轮复审（P1 修复后，CR，reviewer）

VERDICT: approve
Findings: none（第 2 轮 P1 已按最小修复闭合；第 2 轮 P2 报告陈旧已由 lead 授权的正文刷新闭合）
Evidence checked:
- 修复核对：`frontend/src/lib/fixture-composition.ts:85-91` `sentenceEndpointIds` 锚定 P1 material fixture 数据集（textSentence→`fixture-id-004`、subtitleSentence→`fixture-id-008`），与 `materialEndpointIds`（:77-83，001/006）同源；P2 reader artifact 身份仅保留于离线 `composeTokens`（:70-74、:121-122）。附带修正：Study/Knowledge 映射改用 `p1SentenceIds`（:59-67），confirmation 占位会话来源句更正为 P1 语义「𠮟られた。」（原误拼接为「次は君の番です！」）。
- 测试核对：`repository-adapters.test.ts:242-283` 改为以单一 P1 自洽数据集 mock 全链路——tokens `fixture-id-004/008`（material/sidecar 001/002、006/007）、annotations `/materials/fixture-id-001`（span 句 `fixture-id-003`）、decisions body `material_id='fixture-id-001'`——并覆盖文本+字幕两条链与 provenance 重写断言；`AppRouter.test.tsx:120-158` 路由级同样以 P1 数据集断言 txt「次は君の番です！」与 subtitle「また寄ってしまった。」完整渲染与 token 按钮。第 2 轮指出的"混合数据集 mock 掩盖矛盾"已消除。
- 锁定边界实测：`git diff HEAD --stat -- backend DESIGN.md prompts docs/LearningJ-plan-v5.md docs/data-model.md frontend/src/lib/p2-integration.contract.test.ts backend/fixtures/reader-fixture.json` 为零；`git status --short` 范围与第 2 轮一致（frontend/** + 本报告 + 既有 CURRENT-PACKETS.md 修改保留 + 未跟踪 demo/、experiments/）。
- 命令复跑（本轮独立执行，非采信摘要）：`cd frontend && pnpm test` — 26 files / 206 tests pass；`pnpm lint` exit 0；`pnpm build` pass（`index-D0HozWmt.js 365.28 kB`）；`cd backend && uv run pytest -q` — 142 passed / 21 xfailed / 1 warning；`cd demo && pnpm typecheck && pnpm build` — pass；`git diff --check` clean。
- 边界扫描：component→shells/react-router 零命中；demo tokenizer 零命中；裸 slice 仅 `text.ts:46` 授权数组切片；TanStack/Zustand 零命中；`sentenceEndpointIds` 无 reader-artifact ID（`fixture-id-003/006` 仅出现于离线 `composeTokens` 与 P1 语义正确的 materialIds/p1SentenceIds/materialEndpointIds）。
Gate assessment:
- P2.5 通过：P1 身份冲突按最小修复闭合（组合 ID 全链路锚定单一 P1 数据集，adapter 与路由两级回归锁定），报告正文已同步。P3a/P3b/P4a/P4b/P5 可按 handoff points 派遣；fixture 来源 Reader 深链对 textSentence/subtitleSentence 在持有 material fixture 的后端上全链路可用。本包改动可由 lead 提交入册。
Residual risks:
- `annotationSentence`、`fixture-ui-text-sentence-3` 与 epub 句无 token 端点映射（reader fixture 亦无捕获），Reader 对其显式 404/错误态——诚实失败而非静默错数据；P3a 接入真实来源链时再决定是否补捕获。
- 本轮证据仍为锁定 artifact 身份推导 + 双层级确定性测试，未做真实后端浏览器级点通；P2 遗留 popover 近似锚定与 409 并发裁定未演练继续挂起。
