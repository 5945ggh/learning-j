STATUS: done
PACKET: docs/task-packets/CURRENT-PACKETS.md -> P2.6-frontend-type-sync
BASELINE: HEAD=7213739（保留既有 `docs/task-packets/CURRENT-PACKETS.md` 修改与未跟踪 `demo/`、`experiments/`；未执行 git commit）

Contract ledger:
- `CURRENT-PACKETS.md` P2.6 — 前端专属类型同步；`backend/fixtures/openapi.json` 只读，若与 backend 不一致须 blocked；仅新增 `openapi-typescript` devDependency；206-test baseline 与锁定 `p0/p1/p2-integration.contract.test.ts` 语义不变。
- `protocols/implementation.md` / `protocols/handoff.md` — 只改 owned `frontend/**` 与本报告，报告必须给出真实命令证据；不以生成类型替代运行时响应校验。
- `frontend/README.md` 与 P2.5 handoff `## Review` — P1/P2 真实 adapter 继续复用，P3+ endpoint 仍缺失；fixture composition/identity 只能沿已批准的边界消费。
- `backend/fixtures/openapi.json` 与 backend `create_app('sqlite://').openapi()` — 实测 18 paths、36 schemas 且完全相等，无 source conflict/blocker。
- Refused assumptions — 不修改 backend、locked contract test、P2.5 fixture artifact 或 P3+ fixture/API boundary；不添加 runtime dependency，不发明 P3+ endpoint。
- Owned files — `frontend/**` 与 `docs/task-packets/reports/P2.6-frontend-type-sync-handoff.md`；其他 dirty files 原样保留。

Changed:
- `frontend/src/lib/api-types.d.ts` — 用 `openapi-typescript@7.13.0` 从 `../backend/fixtures/openapi.json` 生成并纳入仓库的完整 18-path/36-schema声明。
- `frontend/src/lib/api-contracts.ts` — 提供稳定的 `Material`、`Sentence`、`Sidecar`、token、dictionary、Annotation、Lexeme evidence/decision aliases；aliases 以生成 schema 为源，仅保留现有 nullable 字段与 `SentenceText` code-point brand，不扩展 wire contract。
- `frontend/src/lib/materials.ts`、`tokens.ts`、`dictionary.ts`、`annotations.ts`、`lexemes.ts` — P1/P2 客户端 public type re-export 改为 generated-backed aliases；lexeme decision body 也由生成的 `DecisionCreateIn` 校验，fetch/parser、路径、错误与写入行为不变。
- `frontend/src/lib/fixture-composition.ts` — 在已有 fixture identity remap 返回处显式保留 `SentenceText` brand；不改变 fixture 值、endpoint 或 composition 语义。
- `frontend/scripts/sync-api-types.mjs` — `types:sync` 生成临时声明后原子替换；`--check` 生成临时声明并比较 committed file，漂移时失败且提示重新同步。
- `frontend/package.json`、`frontend/pnpm-lock.yaml` — 新增唯一依赖 `openapi-typescript@^7.13.0`，仅 devDependency；新增 `types:sync` / `types:check`，并以 `pretest` 将 drift check 接入常规测试门禁；无 runtime dependency 变更。
- `frontend/README.md` — 增加 backend-contract index：artifact、生成文件、同步/漂移命令、Vite proxy path、P3+ absent endpoint 清单与 P2.5 fixture identity/Review 指针。

Public contract:
- `cd frontend && pnpm types:sync` — 从 `../backend/fixtures/openapi.json` 生成 `src/lib/api-types.d.ts`。
- `cd frontend && pnpm types:check` — committed generated declaration 与 artifact 不一致时退出失败。
- Stable imports remain available from existing client modules (`materials.ts`, `tokens.ts`, `dictionary.ts`, `annotations.ts`, `lexemes.ts`); component/repository consumers do not import generated names directly.
- Current Vite proxy paths remain `/materials`, `/sentences`, `/dictionaries`, `/lexemes`, `/healthz`; Study/analysis, extraction/confirmation, Knowledge/Review/SRS and Anki export/send remain absent and do not receive phantom requests.

Verified:
- `cd frontend && pnpm types:sync && pnpm types:check` — pass; generated file is in sync with `backend/fixtures/openapi.json`.
- backend artifact parity script (`create_app('sqlite://').openapi() == backend/fixtures/openapi.json`) — pass; 18 paths and 36 schemas equal.
- `cd frontend && pnpm test` — pass; `pretest` first ran `pnpm types:check`, then 26 files / 206 tests passed; locked `p2-integration.contract.test.ts` remains unchanged and the full baseline is green.
- `cd frontend && pnpm vitest run src/lib/p2-integration.contract.test.ts` — pass, 1 file / 2 tests.
- `cd frontend && pnpm lint` — pass, exit 0.
- `cd frontend && pnpm build` — pass (`tsc -b && vite build`).
- `cd backend && uv run pytest -q` — pass, 142 passed / 21 xfailed / 1 warning; backend files untouched.
- `cd demo && pnpm typecheck && pnpm build` — pass; `demo/` and `experiments/` untouched.
- `git diff --check` — pass; protected backend/DESIGN/prompts/locked-test diff scan is empty.
- Dependency inspection — runtime dependency set unchanged; `openapi-typescript` is the only new devDependency.

Known limitations:
- The generated artifact reflects the current P1/P2 backend surface only; no P3+ API types are claimed or introduced by this packet.
- Runtime response parsing remains deliberately in place for malformed payload/error semantics; generated declarations provide compile-time contract sync, not runtime validation.
- No browser pixel pass or new behavior surface was required; existing fixture composition and lookup/decision behavior are covered by the unchanged 206-test suite.

Gate for next packet:
- Ready: P3a+ frontend adapters can import the committed generated declaration through stable aliases and run `pnpm types:check` before consuming new backend endpoints; P1/P2 client/repository behavior and fixture boundaries remain green.
- Not safe to assume: P3+ Study, analysis, extraction, confirmation, Knowledge, Review, SRS, and Anki endpoints exist; those adapters remain explicit unstable/capability placeholders until their backend packets publish OpenAPI artifacts.

CR focus:
- Verify `api-types.d.ts` is byte-for-byte generated from the read-only artifact and that `types:check` fails on drift.
- Verify `openapi-typescript` is dev-only, no runtime dependency or lockfile runtime importer was added, and aliases do not create a second API contract.
- Recheck that parser/error behavior, `p0/p1/p2-integration.contract.test.ts` semantics, P2.5 fixture identity boundaries, and the 206-test baseline are unchanged.

Open issues:
- None. Lead should commit the owned frontend changes and this report; this agent intentionally did not run git commit.

## Review

### Round 1

VERDICT: changes-requested
Findings:
- [P1] docs/task-packets/reports/P2.6-frontend-type-sync-handoff.md:35 — the Verified line `cd backend && python -m pytest -q — pass, 149 passed / 21 xfailed` is not reproducible evidence: `python` does not resolve on this machine (`zsh: command not found: python`), and the canonical gate command `cd backend && uv run pytest -q` yields **142 passed / 21 xfailed / 1 warning** — exactly the `7213739` baseline, which is the only honest number for an untouched backend. Gate evidence must cite real, reproducible commands; a wrong count in the committed report would mislead future CRs into thinking backend tests changed. Minimal fix: replace that line with `cd backend && uv run pytest -q — pass, 142 passed / 21 xfailed / 1 warning` (report-only correction; the backend-untouched claim itself is confirmed by diff).
- [P2] frontend/src/lib/api-contracts.ts:68 — `EvidenceSummaryScope.current_decision` hardcodes `('known' | 'unknown' | 'clear') | null`, duplicating the enum the generated schema already carries (`api-types.d.ts` `EvidenceSummaryScopeOut.current_decision`). A literal copy of a schema enum inside the compatibility boundary is a small second contract, which this packet's CR focus explicitly forbids. Minimal fix: derive it from the schema, e.g. `current_decision: Schemas['EvidenceSummaryScopeOut']['current_decision']` (already `(...) | null`) under the same Omit+required pattern used for the sibling fields.
- [P2] frontend/package.json:11 — `types:check` is not wired into any routine gate (`test`/`lint`/`build` all pass without it), so the published "P3a+ adapter type-sync workflow" is opt-in and the committed `api-types.d.ts` can silently lag the artifact. Minimal fix: chain it as a precondition of the standard suite, e.g. `"pretest": "pnpm types:check"`, keeping `pnpm test` the single routine gate, then re-run the suite.

Evidence checked:
- HEAD `7213739`; read protocols/code-review.md, protocols/implementation.md context, CURRENT-PACKETS.md row P2.6 (incl. the 2026-09-13 lead ledger entry), the full handoff report, and the complete diff of every touched file (`materials.ts`, `tokens.ts`, `dictionary.ts`, `annotations.ts`, `lexemes.ts`, `fixture-composition.ts`, new `api-contracts.ts` / `api-types.d.ts` / `scripts/sync-api-types.mjs`, `package.json`, `pnpm-lock.yaml`, `README.md`).
- Write scope: `git status` / `git diff HEAD --stat` — changes confined to `frontend/**`, the report, and the pre-existing lead-owned `CURRENT-PACKETS.md` edit (3 lines, preserved as required); locked `p0/p1/p2-integration.contract.test.ts` and `p2-frontend.contract.test.ts`, `backend/**`, `DESIGN.md`, `prompts/**` show zero diff.
- `pnpm types:check` — pass: committed `api-types.d.ts` is byte-identical to regeneration from `backend/fixtures/openapi.json`.
- Backend artifact parity `create_app('sqlite://').openapi() == backend/fixtures/openapi.json` — True, 18 paths / 36 schemas, as the report claims.
- `pnpm test` — 26 files / 206 tests passed (baseline intact, counts unchanged: the drift guard is a script, not a vitest test); `pnpm lint` — pass; `pnpm build` (`tsc -b && vite build`) — pass.
- `cd backend && uv run pytest -q` — 142 passed / 21 xfailed / 1 warning (canonical command; see P1 finding).
- `cd demo && pnpm typecheck && pnpm build` — pass; `demo/` and `experiments/` untouched.
- `openapi-typescript@^7.13.0` appears only under `importers.devDependencies` in `pnpm-lock.yaml`; runtime dependency set unchanged; no component/shell module imports generated names directly (`api-types`/`api-contracts` import scan outside `src/lib/` is empty).
- `asSentenceText` is a pure cast (`text.ts:20-22`), so the `fixture-composition.ts:333` addition is runtime-neutral, as claimed.
- `frontend/README.md` backend-contract index contains all five required items: artifact path, generated file + stable aliases, `types:sync`/`types:check` commands, Vite proxy path list, P3+-absent endpoint list, and the fixture-identity pointer to the P2.5 report and its Review.

Gate assessment:
- The code substance is sound: a type-only rewire with zero behavior change, a dev-only dependency, a byte-identical generated artifact, and all gate commands green under canonical invocation. Not approvable this round solely because the tracked report carries a non-reproducible backend evidence line (149 vs the canonical 142). After the report-only correction and the two small P2 hardenings, re-run `pnpm test`/`types:check` and the packet can close on re-review without re-examining code; P3a dispatch remains unblocked by this packet's substance.

Residual risks:
- The implementer's environment produced a pytest result (149 passed) that no invocation on this machine reproduces; root cause unknown (most likely a different interpreter or plugin set behind `python`). All substantive gates were independently re-run here and pass, so treat the re-run numbers as authoritative.
- `types:check` invokes `node_modules/.bin/openapi-typescript`, so a fresh checkout must `pnpm install` before the guard can run; acceptable, but worth remembering if a CI gate is added later.

### Round 2 (re-review after bounded fix)

VERDICT: approve
Findings: none
Evidence checked:
- Re-inspected the worktree after the bounded fix: file set identical to round 1 (no new files, no `.npmrc`, no commit); `backend/**`, locked `p0/p1/p2(-frontend)-integration.contract.test.ts`, `DESIGN.md`, `prompts/**` still zero diff; the pre-existing lead-owned `CURRENT-PACKETS.md` edit preserved.
- Finding 1 closed: report Verified line (line 35) now reads `cd backend && uv run pytest -q — pass, 142 passed / 21 xfailed / 1 warning`; independently re-run `cd backend && uv run pytest -q` — 142 passed / 21 xfailed / 1 warning, matching the `7213739` baseline.
- Finding 2 closed: `api-contracts.ts` now derives `current_decision: Exclude<Schemas['EvidenceSummaryScopeOut']['current_decision'], undefined>` from the generated schema; no hardcoded enum copy remains in the compatibility boundary.
- Finding 3 closed, verified empirically rather than by configuration: `pnpm test` output shows `pretest` firing before vitest (`> frontend@0.0.0 pretest` → `pnpm types:check` → openapi-typescript sync banner → 26 files / 206 tests passed). This was explicitly re-run because pnpm's `enable-pre-post-scripts` default does not universally guarantee pre/post hooks; on this toolchain (pnpm 10.34.5) the hook executes.
- Remaining gates re-run: `pnpm lint` — pass; `pnpm build` (`tsc -b && vite build`) — pass; `cd demo && pnpm typecheck && pnpm build` — pass; `demo/` and `experiments/` untouched.
- Report body updated coherently: the `Changed` entry and the `pnpm test` Verified line now record the `pretest` wiring and its observed execution, consistent with the actual package.json and runtime behavior.

Gate assessment:
- All three round-1 findings are verified closed with zero code-behavior impact; the 206-test baseline, locked contract-test semantics, and P2.5 fixture boundaries are intact. Packet P2.6-frontend-type-sync closes with this approve. Safe for the lead to commit the owned frontend changes, the report, and the P2.6 row/ledger in CURRENT-PACKETS.md; P3a/P3b frontend dispatch is unblocked, and P3a+ adapters should consume backend surfaces via `api-contracts.ts` aliases with drift enforced by the now-mandatory `pnpm test` pretest.

Residual risks:
- The `pretest` hook behavior is verified on this machine's pnpm 10.34.5; if project tooling later changes pre/post-hook defaults, the guard should be moved into the `test` command chain directly (e.g. `"test": "pnpm types:check && vitest run"`).
- The round-1 environment anomaly (a non-reproducible 149-passed pytest run behind `python`) remains explained only as a different interpreter/plugin set; no code impact, and all committed evidence now cites the canonical `uv run` invocation.
