# prompts 目录：版本化的 prompt 模板

文件名即 `prompt_version`（如 `analysis_v1.md`、`extraction_v1.md`），
写入 `Analysis.prompt_version` / `ExtractionRun.prompt_version`。

**纪律**（`docs/mvp-tech-and-phases.md` §1.3）：改 prompt 必须新建文件、
不得原地改写——「永不原地改写」在 prompt 侧的形式。
P3/P4 才开始产出 prompt 文件；本阶段仅建立目录。
