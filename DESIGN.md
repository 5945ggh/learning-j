# LearningJ 界面与交互契约

## 权威来源
- 状态：生效中（MVP 交互契约）
- 最近刷新：2026-09-14（视觉层次、reader chrome 与 Web 材质规则收敛；读者交互模型修订仍以 2026-09-13 记录为准）
- 文档语言：2026-09-10 起与其余契约文档统一为中文；引号内为面向用户的界面标签原文。
- 设计方向更新：应用外壳与素材库借鉴 Apple Books 的信息层级、导航组织与内容优先观感，但不追求材质拟真或对 Apple UI 的像素级模仿。默认方向是平、静、轻分区；正文优先；工具只在真正覆盖正文时获得空间层次。
- 部署形态：web 应用是一等部署形态，可克隆仓库直接部署；打包发行版（倾向 Tauri）只是外层壳切片，共享同一前端与本地后端，不引入第二套业务路径。本设计同时约束两种形态，不过后续关于打包发行版的设计，不排除需要根据使用反馈额外调整。
- 契约同步：学习会话、Agent、历史、证据、查询投影与 Occurrence 级复习对象（[ADR-041](docs/adr/041-occurrence-review-targets.md)）的变更已反映到本文档。2026-09-11 审计补充了开发 schema 基线（[ADR-042](docs/adr/042-development-schema-baseline.md)）与确认快照字段落点；任务包状态以 `CURRENT-PACKETS.md` 及对应交接报告为准，不能据其旧内容判断当前界面契约。顶级导航在本文档确定（“主页”“素材库”“知识库”“解析队列”“学习中心”），主页默认内容仍登记为产品规划 §15.23 的未决项。
- 主要产品界面：应用外壳、素材库、素材阅读器／播放器、句子解析与查词、AI 学习工作区、已提取知识回看
- 已核对的证据：`docs/mvp-tech-and-phases.md`、`docs/LearningJ-plan-v5.md`、`docs/adr.md`、`docs/data-model.md`、`docs/prompt-contracts.md`
- 已接受的视觉与交互参考：以 Apple Books 及相关 Apple 原生应用作为冷静层次、侧边导航、阅读器 chrome、克制控件与内容优先界面的参考；借鉴结构与组织，不复制渐变、阴影或专有材质。`experiments/frontend_samples_v2/reader.html` 与 `study.html` 仍是有用的交互 mockup；`player.html` 提供媒体界面参考。它们是 mockup，不是可执行的业务契约。样例不覆盖当前的文档／编辑器对话布局或会话生命周期。

## 品牌
- 气质：专注、有文气、技术可靠、尊重学习者的自主权
- 基线氛围：现代、安静、有触感、像书；界面应当适合长时间阅读，而不是仪表盘或聊天工具
- 信任信号：来源语境可见；词典、解析器与 AI 输出被清楚区分；来源与持久化会被预先说明
- 借鉴：Apple Books、Finder、Photos 及其他 Apple 原生应用可以影响空间层次、排版、控件克制与阅读器组织。借鉴原则，不抄专有资产，也不做精确的视觉克隆。
- 避免：游戏化连续打卡压力

## 产品目标
- 目标：让真实日语素材保持主体地位；让查词与句法查看即时可达；把有意的 AI 学习变成可复用知识；保留对“学什么、复习什么”的控制
- 非目标：取代 Yomitan；自动讲解每一句；规定完整课程；默认对整本书做 AI 分析


## 用户与任务
详细见 docs/LearningJ-plan-v5.md
简单来说：辅助用户消费一部作品，以最小打断解决一个词或结构；深入学习选定的一句；积累可复用的学习证据


## 信息架构
- 应用外壳导航，顺序为：“主页”“素材库”“知识库”“解析队列”“学习中心”。设置入口固定在导航栏底部。
- “主页”当前只保留为顶级导航占位。默认聚合内容、排序、空状态和进入其他入口的优先级尚未决定；在产品规划 §15.23 关闭前，显示明确的“主页内容待定义”占位，不把 `return_position`、队列数量或推断的学习状态冒充为默认首页内容。
- 五个已定义入口是同一条流水线上按用户排程决定切分的同一批记录：“主页”当前只有占位；“素材库”保存输入材料；“解析队列”保存该决定之前尚未完成的学习会话；“学习中心”保存用户已选择要学的内容以及复习与会话历史；“知识库”保存持久知识资产。任何入口都不得引入实体、不得成为 StudySession／AnalysisRevision／提取状态的第二权威，也不得产生重复的待办状态。
- “解析队列”是未完成学习会话（`status = active`）的全局可筛选视图，与素材范围列表使用同一组件与同一查询权威（[数据模型 §4.1](docs/data-model.md#41-studysession学习会话)）。它的导航标签命名的是主要活动，而不是全部内容：一项可能已经处于可讨论、提取中或待确认。阶段是这个队列内部的筛选，永远不是第二个队列。实体保留自己的名称（“学习会话”）；“解析队列”是视图标签，不是第二个实体，也不是改名。早期的宽泛队列标签不再作为面向用户的名称，并与“学习中心”和内部执行任务队列区分。
- 术语分层，使多个“队列”用法彼此分开：实体＝“学习会话”；视图＝全局“解析队列”与素材范围会话列表；计划与排程＝“学习中心”，其到期集合称为“今日到期”；历史＝“学习记录”；内部运行＝“执行任务队列”，永不作为面向用户的标签。未来的推荐能力提出的是待解析的候选句子——不是要学的知识点——其产物汇入“解析队列”。
- “学习中心”是学习计划与复习工作的聚合外壳：新卡引入、到期复习、学习记录，以及进入同一批视图的素材范围入口。新卡与到期复习来自既有 ReviewItem 排程记录；学习记录来自 StudySession（包括已完成与已搁置的会话），不要求存在 ReviewItem。它不新增实体、不新增独立待办状态，也不新增第二份待办清单。
- 素材工作区：以原文或播放器为主，配一个可折叠的侧栏面板栈。面板栈含三个稳定槽位——查词／词典、句子操作、当前材料会话列表；槽位可折叠、不互斥，任何槽位组合下查词内容都必须一步可达。三栏不是必须的。
- 素材库：提供“书目”与“视听”模式切换。书籍材料使用纵向卡片，以封面为视觉锚点，下沿承载简洁元数据，并以三点溢出操作进行元数据编辑及其他材料操作。音视频材料可以使用不同宽高比与播放控件，同时复用同一套素材卡片归属与间距系统。
- 素材卡片：提供材料会话列表入口，并如实标注活动会话总数；用户可以不先打开阅读器就选中一个会话。溢出菜单不得把轻量的材料操作变成隐藏的学习或删除操作。
- Study：桌面主区是当前解析文档；右侧栏是 Agent 对话，含输入框与可见的工具／编辑反馈。紧凑的会话导航器可以折叠或作为面板打开；它不得挤走对话或压缩文档。
- 知识库：全局与素材范围的列表／详情展示 KP、Occurrence、来源、已验证的关系类型、逐 Occurrence 的 `retention_override` 及其继承或覆盖的 KP `default_retention`，以及实际复习状态。AggregateCard 在此处与候选确认中可复用。完整的交互式图谱后置；不得臆造边，也不得承诺自动语法匹配。
- 复习：到期卡片及其记录的来源内容与可选音频，呈现在“学习中心”内部，而不是作为独立导航入口。
- 返回来源会恢复材料、确切 Sentence 与阅读／播放位置。

### 解析队列与学习记录

用“学习会话”表示一次学习活动；它的全局未完成视图在外壳中标注为“解析队列”，实体保留自身名称。材料范围入口称为“材料会话列表”，不另造队列实体。队列是 StudySession 的视图，不是 Inbox 实体，也不是内部生成任务队列。阅读器／播放器与素材卡片入口按材料筛选；全局入口跨材料使用同一批记录。生命周期与查询权威：[数据模型 §4](docs/data-model.md#4-学习会话工作文档与提取)。

- 准备：一行立即存在，并可在生成完成前打开 Study。展示来源语境、真实排队／运行状态与可用草稿。用户可以起草问题；发送讨论需等待首稿提交。
- 讨论：标签“可讨论”，包括用户从未打开过的会话。完整首稿可用；讨论可以改变数量、措辞与结构，也可以只是回答一个问题。
- 提取：标签“提取中”，并标明固定的文档版本。禁用讨论写入。自动重试耗尽时明确显示“自动重试已耗尽”，保留同一 revision，并提供该 revision 的“重试”、“暂时搁置”和“创建关联新会话”出口；用户手动重试仍保持同一 revision。当前不提供同一会话退回 discussion 或忽略失败强制成功。
- 确认：标签“待确认”；候选列表按 KP 折叠以便阅读，但确认单位是 Occurrence。对当前成功 run 的每一个不同 Occurrence，都提供“安排复习”与“仅作参考”两者，并记录逐 Occurrence 的决定。KP 折叠是展示便利，永不改变被确认的内容——即使某个 KP 只显示为一行，它的每个 Occurrence 也各自承载自己的选择。KP 的 `default_retention` 不是继承来的用户决定：它可以显示为当前默认值，也可以预选或灰显，但每个 Occurrence 仍然需要它自己的明确选择。当某个 KP 已带有用户决定时，把该值显示为默认值，并提供逐 Occurrence 的明确沿用；批量“一并沿用并完成”必须列出受影响的 Occurrence 以及它将记录的值。确认提交记录 `retention_override_at_confirmation` 与 `effective_retention_at_confirmation` 快照。允许部分确认与稍后继续。选择“安排复习”时，新建 ReviewItem 与恰一条授权事实同一提交；复用已有非退役 ReviewItem 时保留其准入状态与进度，不重置为 queued，也不新增授权事实。
- 完成：只有当当前 run 的每一个不同 Occurrence 都获得了本次确认，会话才离开活动队列，并保留在“学习记录”。知识库中之后的意愿变更不会让会话重新排队。零候选结果会解释原因并提供“完成学习”。
- 搁置：“暂时搁置”把不想继续的未完成会话移出默认队列而不删除。学习记录提供恢复；搁置前必须结束或取消在途写入。

展示真实的阶段总数、操作进度与局部失败。不显示未读红点、推断的掌握度、逾期债务或清空队列的压力——即使数据存在也不展示。进入页面不是确认。即使某个会话的 KP 在全局已认识，队列项仍是一个会话。

### 学习中心

“学习中心”是用户学习计划与复习工作的外壳。它呈现统一的新卡与到期队列（来自 ReviewItem 排程）、学习记录（来自 StudySession），以及进入同一批视图的素材范围入口。

- 一个队列，两个合法段落。引入新卡与复习到期卡使用同一套 FSRS 记忆模型；唯一的算法分叉是一张卡在拥有任何复习历史之前对其记忆状态的一次性初始化。把“新卡”与“到期复习”呈现为同一次学习中由每日预算与用户筛选驱动的两个带标签段落，而不是两个应用。
- 段落派生。段落从既有排程记录派生，不新增 card-type 字段：“等待新卡配额”是尚未准入排程的项，“新卡”是已准入但尚未评分、因而没有记忆状态的项，“复习”是已评分且记忆状态到期的项（产品规划 §8，[数据模型 §7](docs/data-model.md#7-srs)）。
- 预算与选择。每日新卡配额与到期集合决定今天有什么；用户可以在这个集合内收窄、筛选或重排。对用户已加入但尚未排程的项进行排序属于该配额机制的一部分，不是推荐。后置的推荐能力（ADR-022）位于上游一层，其原因也是结构性的：产品不对整部材料做 AI 预处理，因此学习会话之前不存在知识点，能提出的只有**尚未解析的句子**，并按预估的信息量、难度与密度排序——其产物汇入“解析队列”。它从不推荐要学的知识点，也不属于这个外壳。
- 配额日界。每日新卡配额按用户配置的 IANA `schedule_timezone` 的本地日结算；未配置时使用设备当前 IANA 时区并在排程设置中可见。跨日只重置可用配额。已准入但尚未首评的项保持“新卡”状态，不重新消耗配额；仍 queued 的项跨日保留并继续按首次准入优先级等待。
- 复习阈值。用户配置的到期或回忆阈值，是一个在明确时间点上对 FSRS 可提取性求值的排程参数。它不得被呈现为、也不得与掌握度或综合估计的阈值相混——后者仍未校准（产品规划 §15.16，[数据模型 §10](docs/data-model.md#10-统计口径与跨层依赖)）。
- 无压力。展示今天有什么以及推进到哪里，但永不显示逾期债务、连续打卡压力、推断的掌握度或未读红点——即使底层数据存在且可计算也不显示。空的一天是合法状态，不是失败。
- 学习记录。已完成与已搁置的会话在此以“学习记录”存在，保留它们记录的文档、对话、提取来源与逐 Occurrence 结果。知识库中之后的意愿变更不会让它们重新打开。
- AI 学习工作区不是导航目的地。Study 作为从“解析队列”“素材库”或“学习中心”推入的全屏界面打开，保持同一会话、位置与草稿；窄屏下它在文档与对话之间切换。

### 文档与 Agent 交互

1. “加入 AI 学习”创建一个带来源、模块选择与可选问题的交互式会话；创建后可立即打开 Study，但默认保留当前阅读界面，不强制自动跳转。普通多选会创建多个此类会话，每个仍可讨论。
2. “自动生成并提取”是独立的显式模式。它跳过讨论，首版每会话一句，并且在新卡排程前仍要求用户做出回看选择。
3. Study 打开后，生成输出可以流式呈现为草稿；它被标注为未完成且不可提取。提交后主区显示完整文档，侧栏继续可用于对话。
4. Agent 可以通过工具编辑小节、重组文本并更新有作用域的笔记／画像。展示进行中、已提交、冲突与失败状态；预算耗尽显示为可续接的暂停状态，用户明确继续后恢复同一会话与当前文档 revision，不自动提取。Study 侧栏提供“笔记与画像”管理入口，用户可查看、编辑、合并或删除，并能看到作用域与提交结果。尽可能在编辑过程中保持阅读锚点稳定，并在配置的草稿历史窗口内暴露最近的编辑／撤销信息。
5. “结束讨论并提取”等待或显式取消在途编辑，然后冻结一个完整已提交的文档版本用于提取。导航与关闭应用／浏览器都不会触发该操作。达到执行预算时会暂停并给出继续路径；它不会隐式接受该文档。
6. 既有后端映射可以把来源 Span 导航到正确的小节版本。提取之前不得臆造 kind 或 Span 关联；编辑之后不得复用旧文档版本的过时映射。
7. 确认按 Occurrence 记录复习选择，与“等待新卡配额”和“已加入复习”相区分。选择“安排复习”时，新建 ReviewItem 与恰一条 Occurrence 授权事实同一提交；复用已有非退役 ReviewItem 不重置状态、准入时间或进度，也不新增授权事实。尚未获得新卡配额的项显示为等待配额，而不是已加入。退役后再次加入无论发生在知识库还是会话内都使用 `explicit_readd`；会话内可选地以 `session_confirmation_id` 反向追溯确认。完成会话不依赖配额分配。

### 知识与复习控制

知识库同时保留 srs 与 reference 项。意愿按 Occurrence 决定，KP 的 `default_retention` 充当默认复习策略，Occurrence 以 `retention_override` 继承或覆盖它；局部值与用户是否真的确认过它是两件不同的事实。“仅作参考”暂停受影响 Occurrence 的既有复习。改回 `srs` 时，已有非退役 ReviewItem 按 `admitted_at` 区分：已准入（`admitted_at` 非空）的项恢复为 `active`，不重复消耗新卡配额并保留已有排程与历史；未准入（`admitted_at` 为空）的项恢复为 `queued`，仍等待首次新卡配额。无 ReviewItem 的 Occurrence 仍须用户明确加入，不因 KP 意愿自动建卡。确认 srs 但没有可用配额的 Occurrence 以 queued 等待；仅凭 KP 默认复习策略永不授权建卡。不要把 reference 标为已删除、被否决的知识或学习失败。

计数与关系要标明各自的证据：已记录的讲解、算法材料出现次数、来源链接与显式合并是不同的事实。完整学习记录仍可从每个来源 Occurrence 访问。用户意愿、排程状态与掌握估计是不同的展示。

### 证据、覆盖与查询反馈

- 分别展示材料出现次数、已记录的证据覆盖与合格词汇卡的复习估计。首版证据覆盖使用数据模型 §10 定义的 token 分母；若要展示按不同 Lexeme 的替代口径必须显式标注。没有证据意味着估计尚未建立，而不是掌握度为零。
- 证据详情标明真实来源与观测时间。允许撤回错误断言或导入来源。“目前不认识”在其作用域内优先于导入与 SRS；“清除我的判断”移除该作用域的覆盖，但不复活更早的断言。按词形的清除仍可继承 Lexeme 级决定；应展示剩余作用域，而不是静默改变其含义。
- 不要把复习选择、暂停、退役或成功生成等同于“认识”这个词。导入回看会预览所选已知词表范围以及未消解／歧义／多 token 计数。普通牌组不会自动成为已知词表。重复上传不会静默恢复已撤回的来源。
- 立即展示已提交的用户判断。后台摘要更新不得临时反转它。导入与重建界面展示真实进度、重试／取消状态与原子发布；不得把部分计数当作完整结果暴露。
- 动态 SRS 估计带有求值时间。过期结果与重建中的摘要显示为更新中／不可用，或一个显式标注日期的先前结果；重建期间永不显示伪造的零。以有界批次刷新可见数据，而不是每个 token 一次请求。
- LexemeActivity 与阅读曝光后置。“材料中出现”由内容索引支撑；“你读过／见过”需要未来的阅读／活动生产者。返回位置不提供该证据。
- 汉字与表记学习、候选排除与提取恢复仍是产品规划 §15 的待决事项。当前确认流程仍然是确认当前 run 的每一个不同 Occurrence，或搁置会话；KP 级折叠是展示便利，不是完成单位，reference 也不是否决某个提取候选。

## 设计原则
- 查词让来源句子在素材工作区保持可见。Study 复现同一段 Sentence 文本、保留材料语境并提供精确返回；它不要求整个阅读器同时留在 AI 文档旁边。
- 交互成本是价值信号。轻量查词是短暂的；显式开始 AI 学习标记的是有意的学习。
- 不与浏览器 Yomitan 手势竞争。LearningJ 使用显式点击／触摸手势，不宣称占用 Option/Alt 加悬停路径。
- 收藏与学习是两个不同的动词。查词不是 KnowledgePoint，也不排程复习。
- 状态转换要可读：阅读、查词、专注学习与知识沉淀使用不同的标签与界面。

## 视觉语言
- 颜色：象牙色与深灰构成默认主题。中性色负责结构；算法／词典与 AI 强调色保持语义化与低饱和，而不是定义整个调色板。琥珀与绿色表达带标签的状态，不是另一套学习内容分类。导航选中态不复用算法／词典或 AI 强调色。
- 基线 token 参考值：`canvas` `#EEEAE2`、`shell_surface` `rgba(255, 255, 255, 0.62)`、`page` `#FFFCF5`、`grouped_surface` `#F3F0E9`、`secondary_grouped_surface` `#E8E3DA`、`text` `#292929`、`secondary_text` `#6F6B64`、`divider` `rgba(41, 41, 41, 0.12)`。这些是初始参考，不能代替对比度测试。
- 语义强调色参考值：算法／词典使用克制的蓝，AI 使用克制的紫；状态色保持带标签且低面积。强调色值必须定义为语义变量，使未来的主题包能够替换它们而不改变组件逻辑。
- 深色与替代主题：不要把浅色主题的值硬编码进组件。为 `canvas`、`shell_surface`、`translucent_surface`、`opaque_surface`、`page`、`grouped_surface`、`secondary_grouped_surface`、`text`、`border`、`shadow`、`algorithm`、`AI`、`warning` 与 `success` 定义语义 token。未来的配色／样式包可以改变这些变量，同时保持交互状态与可读对比。句子语境边缘态与多选入集标记同样定义为语义 token，保证主题可替换、对比度可测与减少动态效果下的退化。
- 材质与层次：Web 版不以 `backdrop-filter`、毛玻璃或桌面透出作为成立前提。`shell_surface`／`translucent_surface` 是 shell、浮动 toolbar、popover 和临时工具 panel 的可选增强；阅读正文、词典释义、AI 解析文档、长文本面板与素材卡片主体保持不透明或近乎不透明。即使半透明效果不可用，界面仍必须通过留白、细边框、surface tone 与排版层级成立。
- 字体排印：原文与引文使用日文衬线体；控件与元数据使用紧凑无衬线体
- 间距／布局节奏：4/8px 基础节奏；工具栏与列表紧凑；阅读栏使用宽松行高
- 形状／圆角／高度：小控件约 7px，操作 10px，分组卡片 14px，阅读纸面可以使用克制的更大圆角。阴影主要表达真实覆盖关系，仅用于 popover、context menu、modal、浮动 toolbar、覆盖正文的临时 panel 或拖拽物体；普通导航项、首页 section、素材卡片、知识库列表、设置项和阅读纸面内部默认无阴影。需要区分边界时，优先使用留白、1px border 与 surface tone。避免把每一段都做成浮动卡片，也不要在密集文本背后加模糊。
- 动效：180–260ms 的不透明度／变换／布局过渡；尊重减少动态效果
- 图像／图标：熟悉的单色符号；素材美术可以标识作品

### 应用外壳与素材界面

- 桌面外壳是带左侧导航栏与宽敞内容区的平静底板；可使用克制的半透明 shell surface 作为空间材质，但不把它作为 Web 版成立前提。不透明或近乎不透明的纸面、卡片、对话框与重文本面板位于其上。
- 导航栏按规定顺序呈现五个主要入口，并让设置入口在视觉上独立位于底部。可使用非交互式分组标题增强“内容／学习”的认知层级，但不得改变导航顺序或引入新的产品入口。选中态使用整行、安静且高对比的表面变化、清晰标签与无障碍状态——不是仅靠颜色；普通项保持透明，hover 只使用轻微 surface tone 变化。
- 素材库与阅读器共同构成首批需要详细设计的界面。素材库的“书目”与“视听”两种模式共用视图切换模式、页边距、搜索／导入入口、空状态与素材卡片契约。
- 书籍卡片为纵向。封面占据卡片大部分，下沿承载名称与最有用的一至两条元数据。卡片使用细边框、低至中等圆角，默认无明显阴影；hover 不做抬升动画，只改变边框、背景或轻微 tonal contrast。三点操作打开材料管理，不与封面或主要打开动作竞争；菜单完整内容不在视觉规范阶段冻结。
- 进入一本书会打开一个位于应用外壳底板之上的白色或暖白阅读纸面。纸面是阅读器稳定的内容框；背景模糊、装饰美术与外壳动效永不降低文字清晰度，也不得移动阅读锚点。
- 材料特定处理可以随模式或未来样式包变化，但导航、卡片操作、来源语境与返回来源行为保持一致。
- EPUB 正文是受控的 publication surface：后端提供版本化、非脚本的本地章节 representation，reader 自有样式负责呈现正文、图片与 ruby。Sentence/token 组件属于语言工具层，不再等同于 EPUB 正文；publication DOM 只在 reader adapter 内映射到 canonical text，不直接成为持久领域坐标。

### 阅读器 chrome 与工具层

- 阅读器使用应用外壳之外的专用 reader chrome。进入阅读器后，应用级 sidebar 默认退出视野，由 reader rail 接管左侧工具空间；返回应用层级通过顶部 toolbar 完成。正文是第一层且永远是视觉主体；顶部 toolbar 与左侧窄功能 rail 是弱于正文的常驻层；工具层只在需要时出现。
- reader rail 与 panel stack 的职责分开。稳定 panel slots 只有三个：查词／词典、句子操作、当前材料会话列表。目录、搜索、排版和标注管理等其他工具可以由 reader rail 触发，但使用自身的 popover、modal、toolbar 或其他临时 surface，不进入稳定 panel stack。
- 顶部 toolbar 负责阅读环境、文档级操作与返回应用层级；reader rail 负责当前材料内的工具切换，不成为第二套产品导航。rail 的最终常驻图标数量、固定尺寸和非稳定工具的具体呈现仍待高保真探索与真实使用反馈。
- 点击对应稳定槽位的 rail 项，在其右侧打开该 panel；再次点击当前项可收起。稳定 panel stack 继续复用三个既有槽位，不新增第二套导航或状态机。
- 打开工具时应尽量维持阅读列宽、当前句屏幕位置和阅读锚点稳定；工具展开不得让正文承担装饰性模糊或复杂背景。
- AI 操作不作为常驻的大型彩色按钮；“加入 AI 学习”属于 token、句子上下文或句子操作 panel 中的有意动作，与阅读呈现工具分开。

## 组件
- 组件职责：应用外壳／导航栏、素材模式切换、素材卡片、素材溢出菜单、词典查词、token 操作、句子语境与多选、句子操作、会话列表、解析文档／编辑器、Agent 对话、确认、聚合与复习卡片。既有组件名在归属仍然成立时可以保留；不要把组件耦合到 shell。
- 查词状态：关闭；已选 token；加载中；一个或多个词典来源；无结果；需要导入；错误
- 归属：组件保持独立于 shell；由 shell 决定查词渲染为 popover 还是抽屉
- 会话列表、文档编辑器、对话面板与确认组件通过 props 接收作用域、记录与回调。shell 装配它们，互不 import，也不把业务生命周期藏在路由状态里。

## 无障碍
- 目标标准：在可行范围内达到 WCAG 2.2 AA
- 键盘／焦点行为：选中文本与 token 控件键盘可达；Escape 关闭 popover／抽屉；焦点返回到触发它的 token；不要求仅靠修饰键的手势
- 阅读正文的 selection/context menu 在平台支持时应响应 `Shift+F10` 或 Context Menu key；Escape 关闭菜单并恢复焦点，键盘路径与鼠标路径提供相同的核心操作。
- 对比／可读性：颜色标注同时带文字标签、图标或下划线
- 屏幕阅读器语义：查词界面作为 popover 时使用 dialog 语义，作为抽屉时使用 complementary/region 语义；来源、阅读与释义都有显式标签
- 句子边缘标记、多选入集控件与多选模式开关需要无障碍名称与键盘可达；多选模式的进入／退出状态可感知
- 减少动态效果：在 `prefers-reduced-motion` 下禁用非必要过渡

## 响应式行为
- 桌面优先，1180px 及以上；紧凑布局低至 760px；760px 以下为堆叠回退
- 宽屏下，查词位于侧栏面板栈的词典槽（抽屉形态）；外壳空间不足时以锚定 popover 打开
- 窄屏下，查词变为底部面板或全宽对话框；未提供可见关闭／返回操作时永不遮盖所选句子
- 触摸／悬停差异：查词通过点击／触摸工作，永不只靠悬停；紧凑布局中控件至少 40px
- 空间不足时，reader rail 优先收纳进顶部／底部阅读工具入口；760px 以下不要求持续占据左侧固定宽度，但其中的稳定 panel slots 与其他工具仍须通过可见入口保持可达。
- 优先折叠材料队列与 Study 导航。窄屏下用清晰的标签页／返回操作在文档与对话面板间切换；保留草稿、滚动位置与同一会话。不要求右侧栏在移动宽度下保持可见，也不照抄样例的固定宽度。
- 窄布局下面板栈收纳为底部面板／对话框，顶部用分段控件切换槽位，查词为默认段；「任何槽位组合下查词一步可达」的红线在各断点同样成立。

### 工具栏与查词边界

- 工具栏形态与确切分组不被样例冻结。让材料导航、呈现控件与工具可见性保持紧凑，并与有意的 AI 操作分开。图标控件需要无障碍名称与足够点击区域；样例图标尺寸不决定点击区域。普通多选的工具栏开关属于呈现控件，与有意的 AI 操作分开；多选模式内查词让位（见「句子语境与多选」）。
- 查词必须允许更换所选 token，而不必反复从侧栏导航栈退出。如果词条详情替换了算法面板，需保留可访问的 token／语境条，并在关闭时恢复焦点。
- 横排／竖排阅读控件与播放器句子控件与本设计兼容。它们本身不创建学习记录。
- 阅读正文范围内可以使用 LearningJ 自有的 selection/context menu，但选区不直接产生新的领域对象。任何文本选区都可复制；span selection 可以添加标注；“查词”仅在选区能够解析为合法 token／可查询词形时出现，或明确转入现有 token 查词流程；“加入 AI 学习”只用选区确定所属 Sentence 或 Sentence 集合，最终仍按现有 Sentence 契约创建会话，不把任意字符 span 当作学习单位，并向用户说明“为整句创建解析会话”或“批量创建解析会话”（每个会话仍对应一句，分别排队）。该接管只适用于明确定义的 controlled reading surface，不扩展到整个应用。输入框、Agent 对话、文档编辑器、设置页与普通可编辑文本区域保留平台原生行为。平台无法完整替换系统选区 UI 时，核心操作必须通过可见替代入口保持可达，并不得破坏基本复制能力或与 Yomitan 的 Option/Alt + hover 手势竞争。

## 交互状态
- 加载中：展示来源名称与紧凑加载状态，不使句子位移
- 空：说明没有导入的词典匹配，并提供“导入词典”或“重新搜索”
- 错误：保留所选界面并给出局部重试；不丢弃阅读器位置
- 成功：展示词形、读音、来源标签、释义、标签与可选元数据；保留回到句子的链接
- 禁用：解释不可用的 AI 或词典操作；没有 BYOK 时算法阅读仍然可用
- 离线／慢网络：本地阅读器、分词器与已导入词典保持可用；只有依赖网络的 AI 操作显示连接状态

### 查词流程

1. 算法视图渲染可选 token 按钮；单次点击／触摸（聚焦时 Enter/Space）选中一个 token 并打开查词；其所在句随之成为当前上下文句（见「句子语境与多选」）。
2. 宽布局下，查词在侧栏面板栈的词典槽（抽屉形态）打开，使句子与邻近语境保持可见。紧凑布局下以锚定 popover 打开；在移动断点以下变为底部面板／对话框。
3. 选择另一个 token 会在原地更新同一个查词界面。第二次点击不会静默创建收藏或 KnowledgePoint；如需固定结果，是一个显式的后续操作。
4. Escape 关闭界面并把焦点还给触发它的 token。点击外部只关闭 popover；抽屉有显式关闭按钮。
5. 该界面展示词形、读音、每个词典来源标签／版本、释义、标签，以及一个与查词分开的明确“开始 AI 学习”操作。

从查词开始 AI 学习会创建与阅读器相同的异步学习会话，并保留当前阅读语境。“这个我认识”“目前不认识”与“清除我的判断”是 Lexeme 域中的显式用户操作；要展示该操作是针对 Lexeme 还是仅针对本次遇到的词形。Annotation 保持独立。这些操作都不会创建 KP 或 ReviewItem。来源必须指明真实的词典／分析器资源；关于句法或词义的 AI 断言，在没有后端证据支持时不得标注为算法已验证。

### 句子语境与多选

1. 句子语境态是派生态：当前上下文句 ≡ 当前选中 token 所在的句。它随 token 选中出现，随 Escape 关闭、切换 token 或离开素材而更新或消失；不存在独立于 token 选择的第二个选择状态。
2. 上下文句的呈现是克制、持续的句子边缘标记：低面积底色加对称的句首／句尾边缘标记（起止对称，兼容未来竖排），出现时一次 180–260ms 过渡，永不使用循环脉冲。它只标注句子边缘，不覆盖正文。
3. 句首／句尾边缘标记同时是可交互 affordance：点击它选中该句而不选中任何 token，用于直接对一句话发起操作——困惑常来自结构而不是某个词。
4. 句内高亮分层，视觉语法不得混淆：用户标注（Annotation）是 span 级墨色持久标记；Occurrence Span（来自知识库或返回来源的定位）是来源定位标记；当前上下文边缘态是瞬态结构标记。三者可在同一句共存，任何状态不得仅靠颜色区分。
5. 普通多选是显式模式：由工具栏开关进入，不得由点击行为推断。多选模式下 token 选中与查词让位：点击 token 或句子边缘区切换该句的入集状态；入集标记仅在多选模式内出现，退出即消失。多选的终态操作遵循产品规划 §6.3：一次创建 N 个可讨论的学习会话，每会话仍为一句，分别排队。
6. 选择状态是单一状态对象（选中 token 或空 + 句子集合），由阅读器外壳唯一持有，组件经 props 接收；不在路由或组件内复制第二份状态机。
7. 句子操作面板是面板栈的一个槽位，呈现当前上下文句或多选集合的作用域操作：加入 AI 学习、添加标注，以及独立切片交付后的句子合并。合并交互必须：仅对相邻句提供“与上一句／下一句合并”；提交前预览合并后文本，并说明将重新分词、修正词频统计；引用守卫不通过时呈现禁用态与原因（如“该句已有学习记录，不能合并”）；完成后阅读器切换到新句并按新 sidecar 代次渲染。合并操作不出现在查词 popover 中。

## 文案与语气
- 语气：冷静、精确、简洁、尊重学习者
- 术语：“查词”用于词典查询，“算法解析”用于确定性查看，“加入 AI 学习”用于创建会话，“学习会话”用于一次学习活动，“解析队列”用于未完成会话的全局视图，“学习记录”用于会话历史，“学习中心”用于新卡与到期的学习外壳，“知识库”用于持久 KP，“AI 学习”用于文档／Agent 工作区。“安排复习”表达把某个 Occurrence 加入学习计划的意图；它不是“该项已学会”的声明。区分逐 Occurrence 的复习选择与真正已排程的项，也区分 KP 默认复习策略与用户已确认的决定；“已加入复习”只保留给真正已获准入排程的项，而不是仍在等待配额的选择。
- 微文案：命令使用动词；来源与状态显式；不要把查词称为“收藏”或“学习”

## 实现约束
- 框架／样式：Vite + React + TypeScript；Tailwind + shadcn/ui；组件独立于 shell
- 词典契约：Yomitan ZIP 是导入格式，不是 UI 或领域模型；API 返回来源／版本信息与文本／结构化释义
- 兼容性：不拦截 Option/Alt + 悬停、浏览器扩展消息或 Yomitan 的修饰键手势
- 部署与媒体：web 应用为一等部署形态，打包发行版只是外层壳切片，共享同一前端与本地后端；媒体一律由本地后端以 HTTP Range 流式提供，播放器组件只接受后端 URL，不读取文件路径。该路径在两种形态下一致
- 浏览器刷新与后退：已提交内容（解析文档、消息、会话与队列状态）以服务端为权威，刷新后恢复；未发送的输入草稿与瞬时选择态在 MVP 不承诺保留，界面不得暗示其已保存。浏览器后退／前进与应用内导航等效；导航与关闭应用／浏览器都不结束讨论、不触发提取（既有契约不变）
- 性能：token 选中后，打开查词界面应是本地且即时的；词典搜索在导入后是本地操作
- 样例能力边界：GiNZA／依存可视化仍为条件式；语法模式自动再遇仍在 MVP 之外。既有词汇匹配与已记录的 Occurrence 可以在标明来源的前提下展示，但不承诺在 AI 提取之前产生新的语法匹配。
- 测试预期：验证点击／触摸查词、键盘调用、Escape／焦点返回、popover 到抽屉的响应式切换、无结果／错误状态，以及与浏览器 Yomitan 扩展共存；阅读正文的 selection/context menu 仅在受控阅读表面接管，并验证复制、合法 token／词形查词、任意 span 标注、整句／批量创建解析会话、`Shift+F10`／Context Menu key、Escape 与可见替代入口；reader rail 在窄屏收纳后仍能访问三个稳定 panel slots；句子语境边缘态随 token 选中出现与消失、边缘标记选句、多选开关的模式切换与入集、窄屏分段控件
- Study 交互检查：从所有入口打开准备中的会话；提交普通多选且不跳过讨论；在有可见结果的前提下提交 Agent 编辑；导航时保留语境；区分自动重试耗尽与手动重试并提供搁置／关联新会话出口；提取固定版本；为本次会话沿用既有选择并显示两个确认快照；复用已有卡不重置状态或重复授权；在后续知识库变更后让已完成记录留在队列之外；恢复已暂停的复习且不丢失历史。这些是当前的设计检查，不代表后置阶段的验收文档已更新。

## 开放问题
- 唯一的未决事项索引是 `docs/LearningJ-plan-v5.md` §15。它跟踪主页默认内容、工具栏／查词细节、reader rail 的具体常驻工具、selection/context menu 的平台呈现、onboarding 与 Agent 运行默认值。本文档确定的导航标签（“主页”“解析队列”“学习中心”）与“导航不触发提取”已定；主页具体聚合仍由 §15.23 管理。不要把已定的标签或失败出口重新打开，也不要把旧入口名当作现行名称。
- 2026-09-13 修订落定：句子语境与多选、侧栏面板栈、三层句内高亮、刷新／后退承诺、媒体单一路径与 web 一等部署形态。视觉方向的高保真探索与对比度测试待后续轮次进行；句子合并／拆分等数据模型事项由授权审计在 §15 与 data-model 登记，本文只约束其交互形态，不复制字段定义。
